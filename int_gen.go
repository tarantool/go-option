// Code generated by github.com/tarantool/go-option; DO NOT EDIT.

package option

import (
	"github.com/vmihailenco/msgpack/v5"
	"github.com/vmihailenco/msgpack/v5/msgpcode"
)

// Int represents an optional value of type int.
// It can either hold a valid int (IsSome == true) or be empty (IsZero == true).
type Int struct {
	value  int
	exists bool
}

// SomeInt creates an optional Int with the given int value.
// The returned Int will have IsSome() == true and IsZero() == false.
//
// Example:
//
//	o := SomeInt(12)
//	if o.IsSome() {
//	    v := o.Unwrap() // v == 12
//	}
func SomeInt(value int) Int {
	return Int{
		value:  value,
		exists: true,
	}
}

// NoneInt creates an empty optional Int value.
// The returned Int will have IsSome() == false and IsZero() == true.
//
// Example:
//
//	o := NoneInt()
//	if o.IsZero() {
//	    fmt.Println("value is absent")
//	}
func NoneInt() Int {
	return Int{
		exists: false,
		value:  zero[int](),
	}
}

// IsSome returns true if the Int contains a value.
// This indicates the value is explicitly set (not None).
func (o Int) IsSome() bool {
	return o.exists
}

// IsZero returns true if the Int does not contain a value.
// Equivalent to !IsSome(). Useful for consistency with types where
// zero value (e.g. 0, false, zero struct) is valid and needs to be distinguished.
func (o Int) IsZero() bool {
	return !o.exists
}

// IsNil is an alias for IsZero.
//
// This method is provided for compatibility with the msgpack Encoder interface.
func (o Int) IsNil() bool {
	return o.IsZero()
}

// Get returns the stored value and a boolean flag indicating its presence.
// If the value is present, returns (value, true).
// If the value is absent, returns (zero value of int, false).
//
// Recommended usage:
//
//	if value, ok := o.Get(); ok {
//	    // use value
//	}
func (o Int) Get() (int, bool) {
	return o.value, o.exists
}

// MustGet returns the stored value if it is present.
// Panics if the value is absent (i.e., IsZero() == true).
//
// Use with caution — only when you are certain the value exists.
//
// Panics with: "optional value is not set" if no value is set.
func (o Int) MustGet() int {
	if !o.exists {
		panic("optional value is not set")
	}

	return o.value
}

// Unwrap returns the stored value regardless of presence.
// If no value is set, returns the zero value for int.
//
// Warning: Does not check presence. Use IsSome() before calling if you need
// to distinguish between absent value and explicit zero value.
func (o Int) Unwrap() int {
	return o.value
}

// UnwrapOr returns the stored value if present.
// Otherwise, returns the provided default value.
//
// Example:
//
//	o := NoneInt()
//	v := o.UnwrapOr(someDefaultInt)
func (o Int) UnwrapOr(defaultValue int) int {
	if o.exists {
		return o.value
	}

	return defaultValue
}

// UnwrapOrElse returns the stored value if present.
// Otherwise, calls the provided function and returns its result.
// Useful when the default value requires computation or side effects.
//
// Example:
//
//	o := NoneInt()
//	v := o.UnwrapOrElse(func() int { return computeDefault() })
func (o Int) UnwrapOrElse(defaultValue func() int) int {
	if o.exists {
		return o.value
	}

	return defaultValue()
}

// EncodeMsgpack encodes the Int value using MessagePack format.
// - If the value is present, it is encoded as int.
// - If the value is absent (None), it is encoded as nil.
//
// Returns an error if encoding fails.
func (o Int) EncodeMsgpack(encoder *msgpack.Encoder) error {
	if o.exists {
		return newEncodeError("Int", encodeInt(encoder, o.value))
	}

	return newEncodeError("Int", encoder.EncodeNil())
}

// DecodeMsgpack decodes a Int value from MessagePack format.
// Supports two input types:
//   - nil: interpreted as no value (NoneInt)
//   - int: interpreted as a present value (SomeInt)
//
// Returns an error if the input type is unsupported or decoding fails.
//
// After successful decoding:
//   - on nil: exists = false, value = default zero value
//   - on int: exists = true, value = decoded value
func (o *Int) DecodeMsgpack(decoder *msgpack.Decoder) error {
	code, err := decoder.PeekCode()
	if err != nil {
		return newDecodeError("Int", err)
	}

	switch {
	case code == msgpcode.Nil:
		o.exists = false

		return newDecodeError("Int", decoder.Skip())
	case checkNumber(code):
		o.value, err = decodeInt(decoder)
		if err != nil {
			return newDecodeError("Int", err)
		}
		o.exists = true

		return err
	default:
		return newDecodeWithCodeError("Int", code)
	}
}
